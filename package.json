{
  "name": "env-json-action",
  "scripts": {
    "build": "ncc build src/index.js",
    "build:watch": "npm run build -- --watch",
    "lint": "npx eslint src"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3",
    "eslint": "^9.20.1",
    "prettier": "^3.5.0"
  }
}
